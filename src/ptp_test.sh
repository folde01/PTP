#!/usr/bin/env bash

# For testing Sniffer class

# Instructions: first start up virtual device (ptp_emulator.sh) as Sniffer.stop()
# needs it to be running (bug).

sudo PATH=$PATH python ptp_test_sniffer.py
